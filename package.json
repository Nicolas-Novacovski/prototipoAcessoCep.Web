{
  "name": "sistema-acesso-cep",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && rsync -a --exclude=public --exclude=.git --exclude=node_modules . public/"
  },
  "devDependencies": {},
  "dependencies": {}
}